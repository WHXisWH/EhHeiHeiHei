rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Backend-only access — all reads/writes come from Cloud Run with ADC
    // No client-side SDK access needed
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
